{
  "name": "power-assert-runtime",
  "description": "monorepo of power-assert runtime side",
  "devDependencies": {
    "babel-core": "^6.1.0",
    "babel-plugin-espower": "power-assert-js/babel-plugin-espower",
    "empower-core": "^0.6.0",
    "lerna": "^1.1.2",
    "mocha": "^2.4.5"
  },
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "mocha packages/*/test/*.js"
  }
}
